import pandas as pd
from datetime import date

def hello(name):
          name = str(name)
          now = pd.to_datetime(str(date.today())

          return name + ' ' + str(now) + ' !'
                               print(hello('hamza'))

output: hamza 6-jan-2023
